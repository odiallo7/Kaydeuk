{% extends 'base.html.twig' %}

{% block title %}Kay Deuk{% endblock %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>Agence Kay Deuk !</h1>
        <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Cum, assumenda id. 
        Doloribus incidunt alias molestias quod aspernatur debitis asperiores quisquam excepturi tempore! 
        Ad voluptatum molestiae mollitia voluptas ullam. Alias, accusantium!</p>
    </div>

    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex" style="margin-left:10px; margin-right:10px">
        {% for property in properties %}
            <div class="clo-3 m-4" >
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"><a href="{{ path('property_show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a> </h5>
                        <p class="card-text">{{ property.city }}({{ property.postalCode }})</p>
                        <div class="text-primary" style="font-weight: blod; font-size: 2rem;">{{ property.formattedPrice }} FCFA</div>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
    </div>

{% endblock %}
